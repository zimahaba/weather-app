import styled from "styled-components";

const WhiteLabel = styled.label`
  color: #ffffff;
`;

const GrayLabel = styled.label`
  color: #a09fb1;
`;

const DivColumn = styled.div`
  display: flex;
  flex-direction: column;
`;

const DivRow = styled.div`
  display: flex;
  flex-direction: row;
`;

export {WhiteLabel, GrayLabel, DivColumn, DivRow};